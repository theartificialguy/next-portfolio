import React from "react";
import { AnimatedText } from "@/components";

const Blogs = () => {
  return (
    <div className="flex flex-col w-full min-h-screen items-center justify-center">
      <AnimatedText text="Blogs coming soon..." />
    </div>
  );
};

export default Blogs;
